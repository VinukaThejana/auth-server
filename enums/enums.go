// Package enums contains enums
package enums

const (
	// SYS_HEALTH -> denotes the health status of the system
	SYS_HEALTH = "health"
	// SYS_HEALTH_MSG -> denotes the custom health status message of the system
	SYS_HEALTH_MSG = "system_message"
)
